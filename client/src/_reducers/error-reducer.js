import { SHOW_ERROR_MESSAGE } from "../_constants/types";

const initialState = {}; 

export default function(state = initialState, action) {
 

	switch (action.type) {
	  
    // case SHOW_ERROR_MESSAGE:

    //   return  action.payload
      

     
    default:
      return state;
  }
}


